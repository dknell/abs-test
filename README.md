# abs-test

Live-validation target for Dextre's PR Shepherd test. CI runs vitest on every push;
fixture branches (`feat/*`) supply `src/` and test files.
Release note: live shepherd validation run (LIN-live-1781280532559.1).
